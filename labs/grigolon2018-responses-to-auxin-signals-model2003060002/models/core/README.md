# Grigolon2018 - Responses to auxin signals

Tellurium-backed Biosimulant core wrapper for `MODEL2003060002`.

Scientific source of truth:
- Bundled SBML file: `data/MODEL2003060002.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `auxin` maps to SBML `auxin`.
- `iaa` maps to SBML `IAA`.
- `arf` maps to SBML `ARF`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
