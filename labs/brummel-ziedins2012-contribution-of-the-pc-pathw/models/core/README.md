# Brummel-Ziedins2012 - Contribution of the PC pathway to thrombin generation

Tellurium-backed Biosimulant core wrapper for `MODEL1807180002`.

Scientific source of truth:
- Bundled SBML file: `data/MODEL1807180002.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `viii_ica1` maps to SBML `VIII_Ica1`.
- `lca1` maps to SBML `LCA1`.
- `apc_lca1` maps to SBML `APC_LCA1`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
