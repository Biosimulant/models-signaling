# Cao2013 - Application of ABSIS in the the enzymatic futile cycle

Tellurium-backed Biosimulant core wrapper for `BIOMD0000000487`.

Scientific source of truth:
- Bundled SBML file: `data/BIOMD0000000487.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `model_state_s1` maps to SBML `S1`.
- `model_state_s2` maps to SBML `S2`.
- `model_state_s3` maps to SBML `S3`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
