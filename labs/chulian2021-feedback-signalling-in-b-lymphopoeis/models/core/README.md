# Chulian2021 - feedback signalling in B lymphopoeisis

Tellurium-backed Biosimulant core wrapper for `BIOMD0000001056`.

Scientific source of truth:
- Bundled SBML file: `data/BIOMD0000001056.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `model_state_c1` maps to SBML `C1`.
- `model_state_c2` maps to SBML `C2`.
- `model_state_c3` maps to SBML `C3`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
