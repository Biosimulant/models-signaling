# Rutkis2013 - Entner-Doudoroff pathway in Z.mobilis (cell-free)

Tellurium-backed Biosimulant core wrapper for `MODEL1409050001`.

Scientific source of truth:
- Bundled SBML file: `data/MODEL1409050001.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `glucex` maps to SBML `GLCo`.
- `ethanolex` maps to SBML `species_1`.
- `gluccy` maps to SBML `GLCi`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
