# Martinez-Guimera2017 - Generic redox signalling model with negative feedback regulation (Model 2)

Tellurium-backed Biosimulant core wrapper for `MODEL2001080001`.

Scientific source of truth:
- Bundled SBML file: `data/MODEL2001080001.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `inactive` maps to SBML `Inactive`.
- `oxidant` maps to SBML `Oxidant`.
- `aox` maps to SBML `AOX`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
