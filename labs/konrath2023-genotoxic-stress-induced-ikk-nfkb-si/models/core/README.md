# Konrath2023_genotoxic_stress_induced_IKK_NFkB_signaling

Tellurium-backed Biosimulant core wrapper for `MODEL2307130001`.

Scientific source of truth:
- Bundled SBML file: `data/MODEL2307130001.xml`
- Equations, parameters, units, and initial values are not manually reimplemented.

Public headline outputs:
- `bs_p` maps to SBML `BS_P`.
- `parp1` maps to SBML `PARP1`.
- `parp1dsb_b` maps to SBML `PARP1DSB_b`.

Caveat:
- Values are native SBML quantities; equations, parameters, and initial values remain in the bundled source file.
